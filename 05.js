//STRING INDEXING

let name = "Dishant";
console.log(name[0]);

//to get the string length
console.log("length of the string is : " , name.length);

// to access last element of the string
// index is always length - 1 same as arrays concept
console.log(name[name.length-1]);